@model User

<div class="d-flex justify-content-center align-items-center vh-100">
    <div class="card p-4 shadow-lg" style="width: 350px;">
        <h3 class="text-center mb-3">Iniciar Sesión</h3>
        <form asp-action="Login" method="post">
            <div class="mb-3">
                <label asp-for="Username" class="form-label">Usuario:</label>
                <input asp-for="Username" class="form-control" required />
            </div>
            <div class="mb-3">
                <label asp-for="Password" class="form-label">Contraseña:</label>
                <input asp-for="Password" type="password" class="form-control" required />
            </div>
            <button type="submit" class="btn btn-success rounded-pill w-100">Iniciar Sesión</button>
        </form>

        @if (!ViewData.ModelState.IsValid)
        {
            <p class="text-danger text-center mt-3">Credenciales incorrectas</p>
        }

      @*   <div class="text-center mt-3">
            <p>¿No tienes una cuenta?</p>
            <a asp-controller="KDP_EC" asp-action="Register" class="btn btn-outline-primary rounded-pill w-100">Registrarse</a>
        </div> *@
    </div>
</div>